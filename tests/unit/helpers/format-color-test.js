
import { formatColor } from 'political-bustle/helpers/format-color';
import { module, test } from 'qunit';

module('Unit | Helper | format color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatColor([42]);
  assert.ok(result);
});

